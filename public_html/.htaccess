# Sukat Kalusugan - LOCAL XAMPP DEVELOPMENT

RewriteEngine On

# Do NOT force HTTPS during local development.
# The ESP32 connects using plain HTTP.

# Never list directory contents.
Options -Indexes

# Block access to dotfiles.
<FilesMatch "^\.">
    Require all denied
</FilesMatch>